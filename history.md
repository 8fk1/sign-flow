# 更新・変更履歴 (History Log)

## 2026-06-05

### 概要
システム全体の監査を実施し、機能上の不整合およびダークテーマ適用時のデザイン上の問題点を修正しました。

### 変更・対応内容

#### 1. ビジュアル個別押印モードにおけるデータリセットバグの修正 (`renderer.js`)
* **問題点:** ファイルの追加・削除・リネームによって `displayFiles()` が呼び出されると、自動的に `updateActiveMode()` が走り、そこで `initVisualMode()` が無条件で呼び出されるため、作業中の押印・日付テキスト等の配置データがすべて初期化されてしまっていた。
* **対応:** 
  * モードの状態を管理する `currentMode` 変数を導入。
  * `updateActiveMode()` において、他のモードから「個別押印（ビジュアル）」へ新しく切り替わった場合のみ `initVisualMode()` が実行されるように制限。
  * すでにビジュアルモードがアクティブである状態でファイルの増減があった場合は、`syncVisualSettingsWithFiles()` で配置設定データ配列 `visualStampsSettings` のサイズおよびインデックスをファイルリストと同期し、データをクリアせずにプレビューを再描画 (`renderVisualStep()`) するように修正。
  * ファイルが削除された際、`visualStampsSettings` 内の該当するインデックスの設定情報も連動して `splice` で削除するように修正。

#### 2. ビジュアルモードでのファイル名リネーム時の表示同期漏れ修正 (`renderer.js`)
* **問題点:** ビジュアルモード上のファイル名変更インプットから名前を変更して確定した際、メモリ上のファイル名は書き換わるものの、画面上部の「アップロード済みのPDF」一覧の表示名が即座に同期されていなかった。
* **対応:** 変更確定時に `displayFiles()` を明示的に呼び出し、ファイルリスト全体に即座に表示名が反映されるように修正。

#### 3. ダークテーマ適用時のデザイン不整合の解消 (`index.html`, `renderer.js`)
* **問題点:** 一部のフォームやモーダルの背景色・境界線色がライトテーマ用のカラーコード（`#f8fafc`, `#ffffff`, `#e2e8f0`）にハードコードされていたため、ダークテーマ選択時にそこだけ白く浮いてしまう、あるいは枠線が消えてしまう等のデザイン上の不整合が発生していた。
* **対応:**
  * **`index.html`**: 新規登録フォーム、印影作成モーダルのボディおよびキャンバス、座標測定モーダルのボディおよび測定結果表示フィールド、アプリ更新履歴コンテナ (`appHistoryContainer`) のインラインスタイルについて、ハードコードされた色コードを CSS 変数 (`var(--setting-row-bg)`, `var(--bg-color)`, `var(--input-bg)`, `var(--panel-bg)`, `var(--panel-border)`) に置き換え。
  * **`renderer.js`**: 一括設定および個別設定行の動的生成スタイル、日付・テキストの編集ポップアップとインプット要素のインラインスタイルで、ハードコードされた色を CSS 変数に置き換え。

### テスト結果
* Playwright を用いた自動テスト（全13件）を実行し、すべてのテストが正常にパスすることを確認済み。
